# NPM Security Configuration
# Baseado em OWASP Top 10 - Vulnerable Components (#6)

# Audit automaticamente em cada instalação
audit=true
audit-level=moderate

# Não permitir scripts em dependências não confiáveis
ignore-scripts=false

# Usar apenas registry oficial do npm
registry=https://registry.npmjs.org/

# Verificar integridade de pacotes
package-lock=true

# Não salvar credenciais em texto plano
save-exact=false

# Usar versões exatas para produção
# (Comentado para permitir updates de patch via Dependabot)
# save-exact=true

# Timeout para evitar travamentos
fetch-timeout=60000

# Não usar cache global (evitar poluição)
# cache-min=0

# Logs mais verbosos para debugging
loglevel=warn
